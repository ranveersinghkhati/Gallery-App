project folder(local) right click >git bash here or cd "path"

// to make ithub repo from computer
git config --global user.name "Ranveer Singh Khati"
git config --global user.email "ranveersinghkahti20153@gmail.com"

touch .gitignore  // create blank file

// initialise your project git repo // create .git folder
git init

//staging area
git add .

git commit -m "Intial commit"

git status
//noting to commit,working on clean tree // ready to bash to github repo

// link my project to this github repo
git remote add origin git@github.com:ranveersinghkhati/Gallery-App.git

// uploaded to git repo
git push origin master





//generate ssh key
$ ssh-keygen
////
$ ssh-keygen -t ed25519 -C "your_email@example.com"
// path of key /c/Users/ranveer/.ssh/id_rsa.pub

// key
cat <path of id_rsa.pub>
